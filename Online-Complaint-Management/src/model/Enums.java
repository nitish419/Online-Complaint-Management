package model;
public class Enums {
    public enum Role { USER, ADMIN }
    public enum Status { OPEN, IN_PROGRESS, RESOLVED, CLOSED }
    public enum Category { TECHNICAL, INFRASTRUCTURE, SERVICE, OTHER }
    public enum Priority { LOW, MEDIUM, HIGH }
}